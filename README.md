# Ardium UI Icons

## Developer Guide
Icons in SVG form are available in `/raw` folder.

If Angular components are needed for copy-pasting into another project, use `npm run to-angular`. The results will be found in `/projects/icons/src/lib` folder.

To view all icons, start the homepage using `npm run start-homepage`. It will open on port 4242.
